/**
 * @deprecated Bu dosya artık kullanılmıyor — iletişim ve sosyal linkler
 * Super Admin tarafından `SiteSetting` tablosundan yönetiliyor.
 *
 * Yerine: `src/lib/site-contact.ts` (`getSiteContact()`).
 * Admin tarafı: `/admin/settings` → İletişim Bilgileri / Sosyal Medya kartları.
 *
 * Bu dosya kasıtlı olarak boş; eski `NEXT_PUBLIC_CONTACT_*` env değişkenleri
 * artık dikkate alınmıyor — production'a deploy ederken kaldırılabilir.
 */

export {};
